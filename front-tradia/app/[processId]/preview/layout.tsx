export const metadata = {
  title: "Process preview",
  description: "Preview translated document",
};

export default function PreviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
